#include<iostream>
using namespace std;
int main()
{
    int k,l,m,n,d;
    cin>>k>>l>>m>>n>>d;
    int array[d];
    int i,count=0;
    for(i=0;i<d;i++)
    {
        array[i]=i+1;
    }
    for(i=0;i<d;i++)
    {
        if(((array[i]%k)==0)||((array[i]%l)==0)||((array[i]%m)==0)||((array[i]%n)==0))
        {
            count++;
        }
    }
    cout<<count;
    return 0;
}
